using UnityEngine;

namespace Objects
{
    public class AttachObject : MonoBehaviour
    {
        /*private bool _isAttached = true;
        private Transform _attachPoint;
        void Update()
        {
            if (Input.GetMouseButtonDown(0))
            {
                _isAttached = false;
            }
            if (_isAttached)
            {
                //ollowPlayer();
            }
        }

        private void FollowPlayer(Transform _attachPoint)
        {
            this.GetComponent<Rigidbody2D>().velocity = (this.transform.position - _attachPoint.transform.position);
        }*/
    }
}
